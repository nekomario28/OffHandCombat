name: Self-hosted release gate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  release-gate:
    runs-on: self-hosted
    timeout-minutes: 5
    steps:
      - name: Manual workflow placeholder
        run: echo 'Use Build and audit for the active self-hosted verification lane.'
